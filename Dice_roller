import random

def roll_dice():
    while True:
        print("🎲 Rolling the dice...")
        print("You got:", random.randint(1, 6))
        again = input("Roll again? (y/n): ")
        if again.lower() != 'y':
            break

roll_dice()
